# Copy to .env.moltbook next to OpenClaw's docker-compose.yml on the VM (never commit).
MOLTBOOK_PLUGIN_DIR=/home/REPLACE_ME/src/molt-agent
MOLTBOOK_API_KEY=
# MOLTBOOK_API_BASE=https://www.moltbook.com/api/v1
